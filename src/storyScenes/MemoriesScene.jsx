import { useEffect, useState } from "react";

export default function MemoriesScene({
    memories,
    onComplete
}) {

    return (
        <div>
            <p>Temporary</p>
        </div>
    );
}